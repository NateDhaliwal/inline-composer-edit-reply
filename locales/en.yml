en:
  save_draft_button_text: "Save draft"
  composer_edit_reason_title: "Edit reason"
  theme_metadata:
    description:
      Edit a post without having the full composer open, instead having
      the composer appearing lineline in the post.
    settings:
      show_preview: Show the preview pane of the composer when editing.
      manually_set_composer_height: "Manually set the height of the composer. if disabled, it will use the user's current configured composer height."
      composer_height: "Height of composer in pixels. Needs `manually_set_composer_height` to be used, or when user's current configured composer height is unchanged."
